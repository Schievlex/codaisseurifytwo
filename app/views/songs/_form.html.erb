<%= form_for @song do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :album %>
  <%= f.text_field :album %>

  <%= f.label :release_year %>
  <%= f.number_field :release_year %>

  <%= f.submit (@song.new_record? ? "Create" : "Update") %>

<% end %>